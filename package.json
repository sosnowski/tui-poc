{
  "name": "tui-postman",
  "private": true,
  "type": "module",
  "workspaces": [
    "tui",
    "core"
  ],
  "scripts": {
    "dev": "bun run --cwd tui --silent dev",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
